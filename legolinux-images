#!/bin/bash

# dependencies /////////////////////////////////////////////////////////////////

if [ -f "${HOME}"/.local/bin/frobulator ]
then
	rm -r -f "${HOME}"/.local/bin/frobulator
fi

if [[ -z $(command -v frobulator) ]]
then
	if [[ $(id -u -n) = "root" ]]
	then
		SUDO_HOME=/root

		USER="${SUDO_USER}"

		HOME=/home/"${USER}"
	fi

	if [[ -z $(command -v curl) ]]
	then
		yes | apt-get install curl
	fi

	if [ ! -d "${HOME}"/.local/bin ]
	then
		mkdir -p "${HOME}"/.local/bin
	fi

	curl -s -L get.frbltr.app > "${HOME}"/.local/bin/frobulator

	chmod +x "${HOME}"/.local/bin/frobulator
fi

. "${HOME}"/.local/bin/frobulator

# superuser ////////////////////////////////////////////////////////////////////

# script ///////////////////////////////////////////////////////////////////////

script=$(basename -- "${BASH_SOURCE[0]}")

# version //////////////////////////////////////////////////////////////////////

version="09-27-2023"

# usage ////////////////////////////////////////////////////////////////////////

# prompt ///////////////////////////////////////////////////////////////////////

frobulator.script "Setting up ${script#*-}"

# variables ////////////////////////////////////////////////////////////////////

local_directory=/media/internal/Lego
local_models_directory="${local_directory}"/models

server_directory="${PWD}"
server_models_directory="${server_directory}"/models
server_images_directory="${server_directory}"/images
server_javascript_directory="${server_directory}"/javascript

# defaults /////////////////////////////////////////////////////////////////////

# functions ////////////////////////////////////////////////////////////////////

# background ///////////////////////////////////////////////////////////////////

# prepare update

frobulator.directory "${server_images_directory}" background

# values

extensions_list+=(
	png
)

# copy images

frobulator.fwd "Copying..."
echo

for extension in ${extensions_list[@]}
do
	directories_list=(
		flat
		overlay
		thumbnail
	)

	for directory in ${directories_list[@]}
	do
		frobulator.directory "${server_images_directory}" "${directory}"

		# copy renders into images directory

		image_count=0

		# set IFS

		IFS=$'\n'

		# shuffle models directories list:
		# creates random showcase of all models on each update

		for image_path in $(find ${local_models_directory} -type f -iname "${directory}"."${extension}" | sort)
		do
			pad_image_count=$(printf "%03d" "${image_count}")

			# get model base directory from path

			image_directory="${image_path%/*}"

			# get model file name from path

			image_file="${image_path##*/}"

			# strip extension from model file name

			image_name="${image_file%.*}"

			rsync				\
				--archive		\
				--quiet			\
				--recursive		\
								"${image_path}" "${server_images_directory}"/${directory}/${image_name}-"${pad_image_count}"."${extension}" > "${sink}" 2>&1

			((image_count++))

		done

	done

	# reset IFS

	IFS=''

	# update random background generator with new file count

	frobulator.inf "Updating" "[ Images ]"
	echo

	sed													\
		-i												\
		-e "s/var count = .*/var count = ${image_count};/g"	\
														"${server_javascript_directory}"/background.js
done

frobulator.inf "Models" "[ ${image_count} ]"
echo

frobulator.cpt "Complete" "[ Images ]"
echo
